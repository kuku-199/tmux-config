# TMUX 使用教程

## 📖 目录

1. [基础概念](#基础概念)
2. [启动与退出](#启动与退出)
3. [窗口管理](#窗口管理)
4. [窗格操作](#窗格操作)
5. [复制粘贴](#复制粘贴)
6. [会话管理](#会话管理)
7. [常用场景](#常用场景)
8. [快捷键速查表](#快捷键速查表)

---

## 基础概念

### 什么是 TMUX？
TMUX 是**终端复用器**，允许你在一个终端窗口中运行多个终端会话，并可以随时分离和恢复。

### 核心概念
- **会话 (Session)**: 一组相关窗口的集合，可以整体保存和恢复
- **窗口 (Window)**: 类似浏览器的标签页，一个会话可以有多个窗口
- **窗格 (Pane)**: 窗口可以分割成多个窗格，每个窗格是一个独立的终端

### 前缀键 (Prefix Key)
TMUX 的所有快捷键都需要先按**前缀键**：
- **前缀键**: `Ctrl + b` (先按住 Ctrl，再按 b，然后松开)
- 按完前缀键后，再按其他快捷键

> 💡 **提示**: 所有快捷键都需要先按 `Ctrl+b`，松开后再按下一个键

---

## 启动与退出

### 启动 TMUX
```bash
# 直接启动（自动创建新会话）
tmux

# 指定会话名
tmux new -s work

# 恢复到已有会话
tmux attach -t work
```

### 退出 TMUX
```bash
# 方法 1: 在当前窗格输入
exit

# 方法 2: 按前缀键 + &
Ctrl+b, &

# 方法 3: 分离会话 (不关闭，后台运行)
Ctrl+b, d
```

### 分离与恢复
```bash
# 分离当前会话 (会话继续运行)
Ctrl+b, d

# 恢复会话
tmux attach -t 会话名

# 查看所有会话
tmux list-sessions
# 或简写
tmux ls
```

---

## 窗口管理

### 创建窗口
```bash
# 前缀键 + c
Ctrl+b, c
```

### 切换窗口
```bash
# 切换到下一个窗口
Ctrl+b, n

# 切换到上一个窗口
Ctrl+b, p

# 切换到指定编号的窗口 (0-9)
Ctrl+b, 0  # 切换到窗口 0
Ctrl+b, 1  # 切换到窗口 1
Ctrl+b, 2  # 切换到窗口 2
# ...

# 切换到最后一个窗口
Ctrl+b, l
```

### 重命名窗口
```bash
# 前缀键 + , (逗号)
Ctrl+b, ,

# 输入新名称，按 Enter 确认
```

### 关闭窗口
```bash
# 关闭当前窗口 (会提示确认)
Ctrl+b, &

# 或直接在终端输入
exit
```

### 窗口列表
状态栏会显示所有窗口：
```
 thorn | 1:zsh 2:nvim* 3:tmux
                 ^
              当前窗口 (带 * 号)
```

---

## 窗格操作

### 分割窗格

#### 左右分割
```bash
# 前缀键 + \ (反斜杠)
Ctrl+b, \
```

#### 上下分割
```bash
# 前缀键 + - (减号)
Ctrl+b, -
```

### 切换窗格
```bash
# 方法 1: 使用方向键 (需要先按前缀键)
Ctrl+b, 左/右/上/下

# 方法 2: Vim 风格 (推荐)
Ctrl+b, h  # 左
Ctrl+b, j  # 下
Ctrl+b, k  # 上
Ctrl+b, l  # 右
```

### 调整窗格大小
```bash
# 先按前缀键，然后重复按调整键
Ctrl+b, Ctrl+h  # 向左扩大
Ctrl+b, Ctrl+j  # 向下扩大
Ctrl+b, Ctrl+k  # 向上扩大
Ctrl+b, Ctrl+l  # 向右扩大

# 或者使用大写键 (更方便)
Ctrl+b, H  # 向左扩大 5 格
Ctrl+b, J  # 向下扩大 5 格
Ctrl+b, K  # 向上扩大 5 格
Ctrl+b, L  # 向右扩大 5 格
```

### 最大化/还原窗格
```bash
# 最大化当前窗格
Ctrl+b, z

# 再次按恢复原状
Ctrl+b, z
```

### 关闭窗格
```bash
# 直接退出终端
exit

# 或前缀键 + x
Ctrl+b, x
```

### 窗格布局示例
```
分割前:                左右分割后:           上下分割后:
┌────────────┐        ┌─────┬─────┐        ┌────────────┐
│            │        │     │     │        │     1      │
│    终端     │   →    │  1  │  2  │   或   ├────────────┤
│            │        │     │     │        │     2      │
└────────────┘        └─────┴─────┘        └────────────┘
```

---

## 复制粘贴

### 进入复制模式
```bash
# 前缀键 + [
Ctrl+b, [
```

### 选择文本
```bash
# 进入复制模式后，使用 Vim 键位移动光标
h/j/k/l  # 左/下/上/右

# 开始选择
v         # 从光标处开始选择

# 矩形选择 (块选择)
Ctrl+v    # 切换选择模式
```

### 复制文本
```bash
# 选择完成后，按以下任一键复制
y         # 复制并取消选择
Enter     # 复制并取消选择
```

### 粘贴文本
```bash
# 前缀键 + P (大写)
Ctrl+b, P
```

### 复制到系统剪贴板
```bash
# 选择后按 y 会自动复制到系统剪贴板
# 可以直接在其他应用中粘贴
```

---

## 会话管理

### 创建会话
```bash
# 创建新会话
tmux new -s 会话名

# 示例
tmux new -s work
tmux new -s project1
```

### 切换会话
```bash
# 分离当前会话，附加到目标会话
tmux switch -t 会话名

# 或在 TMUX 内操作
Ctrl+b, s  # 显示会话列表，用方向键选择
```

### 重命名会话
```bash
# 方法 1: 在 TMUX 内
Ctrl+b, $

# 方法 2: 命令行
tmux rename-session -t 旧名称 新名称
```

### 查看会话列表
```bash
# 在 TMUX 外
tmux list-sessions
# 或
tmux ls

# 在 TMUX 内
Ctrl+b, s
```

### 关闭会话
```bash
# 关闭指定会话
tmux kill-session -t 会话名

# 关闭所有会话
tmux kill-server
```

### 会话持久化
TMUX 配置了自动保存功能：
- 每 15 分钟自动保存一次
- 手动保存：`Ctrl+b, Ctrl+s`
- 手动恢复：`Ctrl+b, Ctrl+r`

---

## 常用场景

### 场景 1: 日常工作流程
```bash
# 1. 早上启动工作会话
tmux attach -d work
# 或创建新会话
tmux new -s work

# 2. 分割窗格
Ctrl+b, \     # 左右分割
Ctrl+b, l     # 切换到右边窗格
Ctrl+b, -     # 右边再上下分割

# 3. 各个窗格运行不同任务
# 窗格 1: 写代码
# 窗格 2: 运行服务器
# 窗格 3: 查看日志

# 4. 午休时分离会话
Ctrl+b, d

# 5. 下午恢复会话
tmux attach -t work
```

### 场景 2: 多项目管理
```bash
# 项目 A
tmux new -s projectA

# 项目 B (新窗口)
tmux new-window -t projectA

# 切换项目
Ctrl+b, s     # 选择项目窗口
# 或
Ctrl+b, 0     # 切换到窗口 0
Ctrl+b, 1     # 切换到窗口 1
```

### 场景 3: 远程工作
```bash
# 1. SSH 到远程服务器
ssh user@server

# 2. 启动 tmux 会话
tmux new -s remote

# 3. 开始工作...

# 4. 网络中断或需要离开
# 直接关闭终端即可，会话会自动保存

# 5. 重新连接后恢复
ssh user@server
tmux attach -t remote
```

### 场景 4: 前后端开发
```bash
# 创建开发环境
tmux new -s dev

# 窗格 1: 前端开发
# 左右分割
Ctrl+b, \

# 窗格 2: 后端开发
# 上下分割
Ctrl+b, -

# 窗格 3: 数据库/日志
# 切换和查看
Ctrl+b, h/j/k/l
```

---

## 快捷键速查表

### 基础快捷键
| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `Ctrl+b, ?` | 帮助 | 显示所有快捷键 |
| `Ctrl+b, r` | 重载配置 | 重新加载配置文件 |
| `Ctrl+b, d` | 分离 | 分离当前会话 |
| `Ctrl+b, s` | 会话列表 | 选择其他会话 |

### 窗口快捷键
| 快捷键 | 功能 |
|--------|------|
| `Ctrl+b, c` | 新建窗口 |
| `Ctrl+b, ,` | 重命名窗口 |
| `Ctrl+b, &` | 关闭窗口 |
| `Ctrl+b, n` | 下一个窗口 |
| `Ctrl+b, p` | 上一个窗口 |
| `Ctrl+b, l` | 上一个窗口 |
| `Ctrl+b, 0-9` | 切换到指定窗口 |

### 窗格快捷键
| 快捷键 | 功能 |
|--------|------|
| `Ctrl+b, \` | 左右分割 |
| `Ctrl+b, -` | 上下分割 |
| `Ctrl+b, x` | 关闭窗格 |
| `Ctrl+b, z` | 最大化/还原 |
| `Ctrl+b, h/j/k/l` | 切换窗格 |
| `Ctrl+b, H/J/K/L` | 调整大小 |

### 复制粘贴
| 快捷键 | 功能 |
|--------|------|
| `Ctrl+b, [` | 进入复制模式 |
| `v` | 开始选择 |
| `y` / `Enter` | 复制 |
| `Ctrl+b, P` | 粘贴 |

### 会话管理
| 快捷键 | 功能 |
|--------|------|
| `Ctrl+b, $` | 重命名会话 |
| `Ctrl+b, s` | 会话列表 |
| `Ctrl+b, Ctrl+s` | 保存会话 |
| `Ctrl+b, Ctrl+r` | 恢复会话 |

---

## 进阶技巧

### 1. 鼠标支持
配置已启用鼠标，可以直接：
- 点击窗口标签切换
- 拖动窗格边界调整大小
- 滚轮滚动查看历史

### 2. Vim 集成
安装了 `vim-tmux-navigator` 插件，可以在 Vim 和 TMUX 之间无缝切换：
```bash
# 在 Vim 中按 Ctrl+h/j/k/l 可以切换到相邻的 tmux 窗格
Ctrl+h  # 左
Ctrl+j  # 下
Ctrl+k  # 上
Ctrl+l  # 右
```

### 3. 搜索历史
```bash
# 进入复制模式
Ctrl+b, [

# 搜索文本
/搜索关键词

# 下一个匹配项
n

# 上一个匹配项
N
```

### 4. 窗格编号
```bash
# 显示窗格编号
Ctrl+b, q

# 数字键选择对应编号的窗格
1, 2, 3...
```

---

## 故障排除

### 问题：快捷键不工作
**解决**:
1. 确认先按了 `Ctrl+b`
2. 松开后再按其他键
3. 检查是否与其他软件冲突

### 问题：找不到会话
**解决**:
```bash
# 查看所有会话
tmux ls

# 如果会话不存在，重新创建
tmux new -s 会话名
```

### 问题：窗格大小混乱
**解决**:
```bash
# 最大化当前窗格
Ctrl+b, z

# 再次按恢复
# 或手动调整
Ctrl+b, H/J/K/L
```

### 问题：复制粘贴不工作
**解决**:
1. 确保按 `Ctrl+b, [` 进入复制模式
2. 按 `v` 开始选择
3. 按 `y` 或 `Enter` 复制
4. 按 `Ctrl+b, P` 粘贴

---

## 更多资源

- 官方文档：`man tmux`
- 配置说明：`~/.tmux/README.md`
- 配置备份：`~/.tmux.conf.bak`

---

## 总结

TMUX 的核心就三点：
1. **前缀键**: `Ctrl+b`
2. **窗口**: 用 `c/n/p/0-9` 管理
3. **窗格**: 用 `\`/`-` 分割，`h/j/k/l` 切换

多加练习，形成肌肉记忆后，效率会大幅提升！🚀
